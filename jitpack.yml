jdk:
  - openjdk17

install:
  - ./gradlew -PGROUP=$GROUP -PVERSION=$VERSION clean :library:assembleRelease :library:publishReleasePublicationToMavenLocal --no-daemon

before_install:
  - ./gradlew clean
