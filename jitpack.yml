jdk:
  - openjdk17

install:
  - ./gradlew -PGROUP=$GROUP -PVERSION=$VERSION clean :library:assembleRelease :library:publishReleasePublicationToMavenRepository --no-daemon

before_install:
  - ./gradlew clean
